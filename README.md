# sense-hat-ip-display

Project to manage raspberry pis with sense hat.

The following scripts exist:
- `src/sense-hat-ip-display.py`: Displays the IP address of the raspberry pi on the sense hat at system start.
- `src/run-ninja-code.py`: Whenever a new *.py file is created inside `~/sense`, this script will run it.
